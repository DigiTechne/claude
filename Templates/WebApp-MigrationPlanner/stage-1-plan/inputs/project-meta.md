# Project Meta

Fill this in before starting Stage 1. One field per line — replace the bracketed prompts.

---

## App type
[PHP + Vite (Agile MVC) / Create React App (CRA) / Other — describe]

## App name
[e.g. BirdDog]

## Target repo name
[e.g. birddog-next — this is what you'll pass to `npm run rename` in the cloned template]

## What the app does
[2-3 sentences. Stage 1 uses this to understand the domain before reading the old code.]

## Snapshot date
[When did you copy the old app into inputs/old-app/? e.g. 2026-05-04]

## What's NEW in v2 (not in the old app)
[List anything you want to add that didn't exist before. Leave blank if this is a straight port.]
- 

## What's being DROPPED (in old app but not wanted in v2)
[List anything from the old app you don't want to carry over.]
- 

## Hard constraints
[Anything Stage 1 must respect — integrations, external APIs, data that must be preserved, etc. Leave blank if none.]
- 

## Deploy target
[e.g. Coolify on Hetzner VPS, or plain Docker + Caddy. Matches the template-next-stack deploy paths.]
